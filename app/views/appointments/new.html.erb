<%= form_for @appointment do |f|%>
  <%= f.label :date_time %>
  <%= f.datetime_select(:date_time, start_hour: 8, end_hour: 18, minute_step:30) %>
  
  <%= f.label 'Visit from a nurse?' %>
  <%= f.check_box(:nurse_help) %>

  Rezerwacje:
  <%= f.label :date_time %>
  <%= f.select(:reservation_id, @reservations) %>

  <%= f.submit 'save'%>
<% end %>


