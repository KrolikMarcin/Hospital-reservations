  <% unless @reservation.bill.nil? %>
    <%= link_to 'Bill', bill_path(@reservation.bill), class: "btn btn-outline-secondary"%>
  <% end %>
  <% if @reservation.status && @reservation.bill.nil? %>
    <%= link_to 'Issue a bill', new_admin_reservation_bill_path(@reservation), class: "btn btn-outline-secondary"%>
  <% end %>
  <%= link_to 'Destroy', reservation_path(@reservation), method: :delete, 
  data: { confirm: 'Are you sure?' }, class: "btn btn-outline-secondary" %>