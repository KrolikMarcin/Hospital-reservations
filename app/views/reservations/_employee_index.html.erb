<div class="container">            
<table class="table table-striped">
  <thead>
    <tr>
      <th>Patient</th>
      <th>Date</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% @reservations.each do |reservation| %>
    <tr>
      <td><%= reservation.patient.full_name %>
      <td><%= reservation.date_formated %></td>
      <td><%= reservation.check_status %>
    <% end %>
    </tr>
  </tbody>
</table>
</div>