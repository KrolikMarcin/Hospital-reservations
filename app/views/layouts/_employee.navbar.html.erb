<ul class='navbar-nav mr-auto'>

  <li class='navbar-item dropdown'>
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      Reservations
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
      <%= link_to 'Today reservations', reservations_path(:today), class: 'dropdown-item'%>
      <%= link_to 'All reservations', reservations_path, class: 'dropdown-item' %>
    </div>
  </li>

</ul>

<ul class="navbar-nav ml-auto">

  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      Profile
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
      
      <%= link_to 'Edit profile', edit_user_registration_path, class: 'dropdown-item' %> 
      <%= link_to 'Destroy', user_registration_path(current_user), method: :delete, class: 'dropdown-item' %>
      <div class="dropdown-divider"></div>
      <%= link_to 'Add address', new_address_path, class: 'dropdown-item'%>
      <% unless current_user.address.nil? %>
        <%= link_to 'Edit address', edit_address_path(current_user.address), class: 'dropdown-item'%>
      <% end %>
    </div>
  </li>

  <li class='nav-item'>
    <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'nav-link'  %>
  </li>
</ul>
